locals {
  s3_buckets = {
    #TODO: import into state once config recorder is unstuck.
    # "config-bucket-959946720450" = {
    #   acl               = "private"
    #   sse_enabled       = true
    #   versioning_status = "Enabled"
    # }
  }
}
